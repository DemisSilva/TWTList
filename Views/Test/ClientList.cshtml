@model ICollection<Client>
<partial   ="_Header"/>
<table class="table">
    <thead>
        <tr>
            <th>Nome</th>
            <th>E-mail</th>
            <th>Compras</th>
            <th>Tipo</th>
        </tr>
    </thead>
    <tbody>
        @* @for(var i = 0; i< Model.Count; i++)
        {
            <tr>
                <td>@Model.ElementAt(i).Name</td>
                <td>@Model.ElementAt(i).Email</td>
                <td>@Model.ElementAt(i).Purchases</td>
                <td>@Model.ElementAt(i).Type</td>
            </tr>
        } *@

        @foreach(var client in Model)
        {
            <tr>
                <td>@client.Name</td>
                <td>@client.Email</td>
                <td>@client.Purchases</td>
                <td>@client.Type</td>
            </tr>
        }
    </tbody>
</table>